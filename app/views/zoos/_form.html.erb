<%= form_with(model: zoo, local: true) do |form| %>
  <% if zoo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(zoo.errors.count, "error") %> prohibited this zoo from being saved:</h2>

      <ul>
      <% zoo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
